# Phase 1 Web MVP 依赖
# 安装：.venv/bin/pip install -r phase1/requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.0
aiofiles>=24.0

# Phase 2: auth + multipart upload
passlib[bcrypt]==1.7.4
bcrypt<4.2  # passlib 1.7.4 跟 bcrypt 5+ 不兼容（detect_wrap_bug 触发 72-byte 限制）
python-jose[cryptography]==3.3.0
python-multipart==0.0.12

# DB driver: 默认 sqlite（stdlib 自带）；切 MySQL 时装 pymysql
#   pip install pymysql
# 切到 MySQL 只需设环境变量 DB_URL=mysql+pymysql://user:pwd@host/db?charset=utf8mb4
pymysql>=1.1
